// ltl-dhcp-probe/main.ltl
//
// DHCP DISCOVER broadcaster + OFFER parser.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-dhcp-probe v0.1.0")
    io::println("DHCP DISCOVER broadcaster + OFFER parser.")
    0
}
